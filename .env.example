# Copie para .env.local se quiser apontar o dev para outro backend.
# Em produção, esta variável é definida no painel da Vercel
# (Project Settings > Environment Variables), não neste arquivo.
#
# Sem barra no final.
VITE_API_URL=http://localhost:8080
